<h1>ショッピングカート</h1>

<p><%= link_to "カートを空にする", cart_items_destroy_all_path, method: :delete, "data-confirm" => "本当に削除しますか？" %></p>

<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価(税込)</th>
      <th>数量</th>
      <th>小計</th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <% @sum = 0 %>
    <% @cart_items.each do |cart_item| %> <%# <=現状@cart.itemsにはユーザの情報しか入れてないから、itemの情報が取れてるかはわかりません %>
    <tr>
      <td><%= cart_item.item.name %></td>
      <td><%= cart_item.item.price * 1.1 %></td>
      <td><%= cart_item.quantity %></td>
      <td><%= cart_item.total_price %></td>
      <td><%=link_to "削除する", cart_item_path(cart_items.id), class: "btn btn-danger", method: :delete,"data-confirm" => "削除しますか？" %></td>
    </tr>
    <% @sum += cart_item.total_price %>
    <% end %>
  </tbody>
</table>

<table>
  <tbody>
    <tr>
      <td>合計金額</td>
      <td><%= @sum %></td>
    </tr>
  </tbody>
</table>


<div>
<%= link_to "情報入力画面へ進む", new_order_path, class:"btn btn-success" %>
</div>